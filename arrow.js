//function soma(x, y) {
    //return x + y;
//}

//console.log(soma(3, 4));

const multiplicacao = function (x, y) {
    return x * y;
}

console.log(multiplicacao(5, 5));

const dividir = (x, y) => {
    return x / y;
}

console.log(dividir(12, 6));

const objecto = () => ({ nome: 'Ana', sobrenome: 'Silva Lopes' });
